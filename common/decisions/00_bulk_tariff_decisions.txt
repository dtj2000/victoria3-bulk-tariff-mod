# Bulk Tariff Management - Single Decision
# Opens a two-page event window (Import / Export) to select
# a tariff/subvention policy to apply to all 49 tradeable goods at once.

bulk_tariff_decision = {
	is_shown = {
		always = yes
	}
	possible = {
		always = yes
	}
	when_taken = {
		trigger_event = { id = btm.1 popup = yes }
	}
	ai_chance = { value = 0 }
}
